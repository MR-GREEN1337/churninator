# --- AI PROVIDER ---
# Switch this to "openai" to use GPT-4o instead of your local model
VLM_PROVIDER=local

# Add your API keys here, they will only be used if the provider is selected
OPENAI_API_KEY="sk-..." # pragma: allowlist secret
