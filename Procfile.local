# Procfile.local
api: uvicorn backend.src.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir backend/src
inference: uvicorn backend.inference.server:app --port 8001
worker: dramatiq backend.worker.broker backend.worker.tasks
frontend: cd web && npm run dev
